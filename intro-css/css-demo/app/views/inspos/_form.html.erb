<%= form_with(model: inspo) do |form| %>
  <% if inspo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inspo.errors.count, "error") %> prohibited this inspo from being saved:</h2>

      <ul>
        <% inspo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :code_snippet %>
    <%= form.text_field :code_snippet %>
  </div>

  <div class="field">
    <%= form.label :img_url %>
    <%= form.text_field :img_url %>
  </div>

  <div class="actions" id="form-submit-container">
    <%= form.submit %>
  </div>
<% end %>
