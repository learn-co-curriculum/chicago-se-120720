import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);


/*
 *
Deliverables:

X Render all of the existing todos on the back end
   X completed todos have there text strikedthrough
X create a form that allows a user to add a new Todo on the BE and FE
X allow a user to delete a todo that removes it from the BE and FE
- Create a filter that allows the user to see just completed todos

Bonus
Create a button that allows a user to mark a todo as completed. Should update both BE and FE
Use Semantic-react for styling

*/
